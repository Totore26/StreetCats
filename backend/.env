# DB 
DB_CONNECTION_URI = "sqlite:database.db"
DIALECT = "sqlite"
# Security
JWT_SECRET = "[2025] GattoJsonWebToken_in_azione 🐱💥 [2025]"
PORT = "3000"
DEV_MODE = "true" # false if we are in production (remove possibility to reset db with /reset endpoint)